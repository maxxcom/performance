json.extract! vfactor, :id, :denominazione, :descrizione, :peso_sg, :peso_dirigenti, :peso_po, :peso_preposti, :peso_nonpreposti, :created_at, :updated_at
json.url vfactor_url(vfactor, format: :json)
